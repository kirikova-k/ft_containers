#include <iostream>
#include "Node.hpp"
#include "Tree.hpp"

int main()
{

	Tree *tree;
	tree = new Tree();
	tree.add()

 Node *Root = 0;
 char s[] = "84527819";

 for (int i = 0; s[i]; i++)
 {
 Add(s[i], Root);
 }

 print(Root);
 FreeTree(Root);

//  cin.get();
 return 0;
}
